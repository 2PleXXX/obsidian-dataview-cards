---
cssclasses:
  - hide-properties
obsidianUIMode: preview
---
# 🧭 [[01 Путеводитель|Путеводитель]] • [[06 🧮 СОРТИРОВКА|← Назад]] • [[08 🖼 ОТОБРАЖАЕМЫЕ ПОЛЯ|Далее →]] • [[00 HUB|Домашняя страница]] 🏠

---
# БЛОК 07 🔐 ФИЛЬТРАЦИЯ

> 💡 **Пример конфигурации блока:**

```js
filtering: {
  saveToLocalStorage: true,
  mode: "byTypes",
  allowedTypes: ["text", "badge", "date", "pageLink", "number", "rating", "progressBar"],
  allowedFields: ["Title", "Badge", "DemoDate", "Rating"],
  defaultMatchMode: "any",
  defaultFilterMode: "whitelist",
},
```

	Этот блок управляет поведением модального окна фильтрации, которое открывается при нажатии на кнопку "Фильтр" расположенное рядом с поисковым полем

---

## 💾 `saveToLocalStorage`

Определяет, сохраняются ли **параметры фильтрации** между сессиями:

- `true` — сохранять в localStorage;
    
- `false` — не сохранять.
    

---

## 🔧 `mode`

Определяет, **что отображается в выпадающем списке выбора полей фильтрации**:

- `"byTypes"` — отображаются поля по их типам (а не названия из YAML);
    
- `"byFields"` — отображаются конкретные поля перечисленные в `allowedFields`.
    

⚠️ Все доступные типы указаны здесь ---> [[02 Типы полей]]

---
## ✅ `allowedTypes`

Массив типов полей, доступных для фильтрации (если `mode: "byTypes"`). Например:

```js
["text", "badge", "date", "pageLink", "number", "rating", "progressBar"]
```

⚠️ Все доступные типы указаны здесь ---> [[02 Типы полей]] 

---
## 📌 `allowedFields`

Массив конкретных полей, которые создаются в [[08 🖼 ОТОБРАЖАЕМЫЕ ПОЛЯ]], доступных для фильтрации (если `mode: "byFields"`). Например:

```js
["Title", "Badge", "DemoDate", "Rating"]
```

---

## ⚙️ `defaultMatchMode`

Определяет режим **логического соответствия** условий фильтрации по умолчанию:

- `"any"` — удовлетворяет хотя бы одному из условий;
    
- `"all"` — должны быть выполнены **все** условия одновременно.
    

---

## 🌓 `defaultFilterMode`

Режим фильтрации по умолчанию:

- `"whitelist"` — показать **только те**, которые соответствуют условиям;
    
- `"blacklist"` — исключить те, которые соответствуют условиям.
    

---

> 🔠 **Фильтрация чувствительна к регистру!** Например, `Tag` и `tag` будут восприниматься как разные значения.

---
# 🧭 [[01 Путеводитель|Путеводитель]] • [[06 🧮 СОРТИРОВКА|← Назад]] • [[08 🖼 ОТОБРАЖАЕМЫЕ ПОЛЯ|Далее →]] • [[00 HUB|Домашняя страница]] 🏠

